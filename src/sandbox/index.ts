/**
 * Sandbox subsystem.
 *
 * Currently only exposes the session tracker for Agent SDK session
 * resumption. Docker container support is deferred (TASK-037).
 */

export { SessionTracker } from './session-tracker.js';
