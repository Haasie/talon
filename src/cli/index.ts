/**
 * talonctl — Talon CLI control utility entry point.
 *
 * Provides sub-commands for managing the talond daemon, channels, personas,
 * migrations, and other administrative tasks.
 */

console.log('talonctl');
