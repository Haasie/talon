/**
 * Telegram channel connector.
 *
 * Implements the Channel interface for Telegram via the Bot API.
 * Handles polling or webhook mode, message normalisation, and send with
 * Markdown-to-MarkdownV2 conversion.
 */

export {};
