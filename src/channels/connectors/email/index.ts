/**
 * Email channel connector.
 *
 * Implements the Channel interface for email via SMTP (outbound) and
 * IMAP/JMAP or inbound SMTP (inbound). Handles MIME parsing and
 * Markdown-to-HTML conversion for rich email sends.
 */

export {};
