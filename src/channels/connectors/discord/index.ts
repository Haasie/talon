/**
 * Discord channel connector.
 *
 * Implements the Channel interface for Discord via the Discord.js library.
 * Handles gateway events, message normalisation, and Markdown pass-through
 * (Discord renders a subset of Markdown natively).
 */

export {};
