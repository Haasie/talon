/**
 * Message format conversion.
 *
 * Converts between the internal Markdown representation and channel-native
 * formats (Telegram MarkdownV2, Slack mrkdwn, plain text, HTML, etc.).
 * Markdown is the canonical output format; channels convert on the way out.
 */

export {};
