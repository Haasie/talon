/**
 * Shared domain types.
 *
 * Branded primitive types (ThreadId, PersonaId, MessageId …) and shared
 * interfaces used across multiple subsystems. Keep this module free of
 * runtime dependencies — types only.
 */

export {};
