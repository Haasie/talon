/**
 * Configuration loading and validation.
 *
 * Reads talond.yaml, parses with js-yaml, and validates against the Zod schema.
 * Exports a strongly-typed Config object for use throughout the daemon.
 */

export {};
